package game.data;

/**
 * Enum containing all type of click we can have
 * when an event regarding the mouse is trigerred.
 * 
 */
public enum ClickType {
  ITEM,
  BAG,
  MAP,
  MAP_OR_BAG,
  EVENT_CHOICE,
  NOTHING;
}
